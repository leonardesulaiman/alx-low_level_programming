#include <stdio.h>
/**
 * main - Prints some letters of alphabet
 *
 * Return: Always (Success)
 */
int main(void)
{
	char ch = 'a';

	while (ch ='a')
		{
			if (ch != 'e' && ch != 'q')
				{
					putchar(ch);
				}
			ch++;
		
	}

	putchar('\n');

	return (0);
}

